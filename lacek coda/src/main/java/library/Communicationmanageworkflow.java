/*package library;
import java.security.InvalidParameterException;
import java.util.concurrent.TimeUnit;

import org.openqa.selenium.By;
import org.openqa.selenium.JavascriptExecutor;
import org.openqa.selenium.Keys;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.support.ui.Select;
import org.testng.Assert;
import Utility.PageBase;



public class Communicationmanageworkflow extends PageBase {

	public Communicationmanageworkflow(WebDriver driver) {
		super(driver);
	/*	// TODO Auto-generated constructor stub
		public Communicationmanageworkflow manageworkflowehiqa (String manageworkflow,String Communicationname)
		{
			readconfig();
			switch(manageworkflow)
			{
			case "DRAFT":
			{
				driver.findElement(By.id(library.getProperty("comm_edit_workflow"))).sendKeys("D");
				break;
			}
			case "APPROVED":
			{
				driver.findElement(By.id(library.getProperty("comm_edit_workflow"))).sendKeys("A");
				break;
			}
			case "Qatest1":
			{
				driver.findElement(By.id(library.getProperty("comm_edit_workflow"))).sendKeys("Q");
				break;
			}
			default: 
			{
				throw new InvalidParameterException("Please verify the workflow name");
			}
			}
			
		}
	}

}*/
